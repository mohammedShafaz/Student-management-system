export enum UserRoles {
    ADMIN = "ADMIN",
    STUDENT = "STUDENT",
    TEACHER = "TEACHER"
}

export enum TaskStatus {
    PENDING = "PENDING",
    OVERDUE = "OVERDUE",
    COMPLETED = "COMPLETED"
}